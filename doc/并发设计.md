# Thread Affinity / Thread Specific Ownership
问：我可以不可以，主线程的每一个对象，都明确只有一个线程访问，也就是说，所有读写操作，都在某一个子线程中？

完全可以，而且这是一个非常优秀、能极大简化并发复杂性的设计思想！

你描述的模式通常被称为 “线程专有所有权”（Thread-Specific Ownership） 或 “线程亲和性”（Thread Affinity）。在这种模式下，每个对象都被明确地绑定到某一个特定的线程，所有对该对象的读写操作都必须在那个线程的上下文中执行。